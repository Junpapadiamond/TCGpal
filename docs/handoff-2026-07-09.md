# TCGpal Session Handoff — 2026-07-09

**Current status (for the next agent picking this up):** No task in flight. All work below is committed and pushed to both `claude/product-base-polish-6327jx` and `main` (HEAD `5b3b877`). Nothing pending, nothing blocked. If the user brings a new bug screenshot, the pattern is: reproduce the exact case in `variant-fidelity.test.ts` first (TDD), then fix in `ranking.ts`/`ebay.ts`. If asked to verify live, see "Unfinished" below — this sandbox has no live eBay/pokemontcg.io access.

Branch: `claude/product-base-polish-6327jx` — fully in sync with `origin/main` (0 ahead, 0 behind). Working tree clean, nothing uncommitted or unpushed. Gate green: lint clean, typecheck clean, 294/294 tests passing.

## Done

| Commit | Outcome |
|---|---|
| `477f94d` | Verdict hero gained a deterministic **Action** line (buy / wait / pass, cautious language) and a **"Why not the cheapest"** column — states the skipped savings and the cheaper copy's specific weakness whenever the pick isn't the cheapest eligible listing. |
| `05750fa` | **Root-caused a One Piece bug**: confirming an SP (Special Art) print could recommend a regular alt-art copy. `VariantIntent` widened from binary base/alt to base/alt/sp/manga/treasure, derived from the catalog's own rarity+variant fields; title gate now requires the exact class and rejects titles carrying a *different* class's marker. |
| `6ad1665` | eBay search queries for non-base One Piece prints now lead with a variant token ("Nami OP01-016 SP"), plain query as fallback — fewer searches return only base-print noise. |
| `25d9d5b` | **Helpful abstention**: when live listings exist but none survive the gates, the report carries a deterministic reason + a one-tap "Compare the \<sibling print\> instead" button, instead of a bare empty state. |
| `e3f5767` | Fix-once regression sweep (`variant-fidelity.test.ts`) covering the SP/alt/base matrix end-to-end; the 5-card product-flow standard gained a 6th step confirming an SP print permanently. |
| `ff2faaa`, `2948b8f`, `c913331` | **Full UI subtraction pass** (per user request to declutter): sticky Q&A column removed → single centered result column, Q&A now on-demand; 3 always-shown candidate rows collapsed into one "Compare N other listings" fold; web-discovery panel removed from UI (backend untouched); decision receipt folded to one status line (still shows sources+market+timestamp when closed); feedback prompt shrunk to a quiet always-visible row; lens controls became compact pills; hero's numeric "strength" chip dropped; landing headline rewritten to lead with the buyer outcome; three new fold-open analytics events added. |
| `79a6a7b` | **Fixed a real reported bug** (user screenshot): a listing whose *title* said "NM-LP" but whose structured condition field said "Near Mint" was recommended to an NM-minimum buyer. Added `titleConditionFloor` — reads the worst condition a title admits (ranges, spelled-out grades, "played", damaged) and gates on the worse of title vs. structured claim. Also dropped the last visible raw N/100 score from the copied receipt (plain-language labels only, per user ask to de-emphasize scores). |
| `aa2d81e` | **Fixed a real reported bug** (user screenshots): the identity-confirmation Set/Rarity/Print-type filters were fully independent dropdowns even though rarity and print-type overlap (SP CARD rarity ⟺ Special Art print type, SEC never coexists with Special Art, etc.), so picking an incompatible combo silently showed "0 versions". New `identity-filters.ts` cross-filters each dropdown against the *other* active filters and auto-clears a sibling filter the moment it becomes unreachable. |
| `f82a1b2` | **Fixed a real reported bug** (user screenshots): "50 listings found, 49 excluded as a different print." Root cause: sellers often leave the eBay title plain while eBay's own item specifics (Parallel/Variant, Rarity) do name the print — the gate only ever read the title. New `matchAspectText` field carries the enriched item-detail `localizedAspects` alongside the title into the variant/language gates. Proven additive-only (never weakens the gate) with a full test matrix + an end-to-end case reproducing the exact scenario resolving into a real recommendation. |

## Problems / blocked

None currently. Everything above is committed, pushed, and gate-verified (lint + typecheck + 294 tests, just re-run this session). `npm run build` was **not** re-run this session (skipped per the skill's guidance to save time) — it was green as of the last push in this conversation; re-run it before deploying if in doubt.

Two known, accepted limitations (not bugs, documented in commit messages):
- eBay's item-detail enrichment (which now feeds `matchAspectText`) is bounded to roughly the top 12 relevance-ranked search results per query, by deliberate design (avoids an unbounded crawl). Listings beyond that window are still title-only for variant/language matching.
- This sandbox cannot reach live eBay/pokemontcg.io (egress-restricted), so all screenshots and manual smoke tests this session ran in `demoMode` against the bundled One Piece catalog. The eBay-live path is exercised only by the hermetic unit/e2e test suite (injected fetchers), not a real API call.

## Unfinished — goals + next steps

Active goal per `/root/.claude/plans/i-feel-like-there-reactive-riddle.md`: make TCGpal's core recommendation "extremely accurate" before letting other people test it, per the user's explicit instruction in this session. The three concrete bugs reported via screenshots are now fixed and regression-tested. No open plan items remain unimplemented from that file.

Natural next steps for whoever picks this up:
1. **Verify live**, with real eBay credentials on a machine with network access: re-run the exact Nami/Charmander/Portgas.D.Ace searches from the user's screenshots and confirm (a) the SP print no longer pulls in alt-art listings, (b) the NM-LP title is excluded, (c) the rarity/print-type filter no longer produces a dead "0 of 77" combo, (d) the "50 found, 49 excluded" case now yields a real recommendation where item specifics support it.
2. Consider raising the item-detail enrichment cap (currently ~12) if live testing shows the correct print is still commonly ranked below it — this is a latency/accuracy tradeoff, not implemented this session.
3. Keep feeding any further false-exclusion screenshots into `variant-fidelity.test.ts` — that suite is now the designated fix-once home for this bug class.

Most useful next action: get eBay credentials into a live-network environment and run the standard 5-(now 6-)card flow plus the three specific cards from this session's screenshots, then compare against the demo-mode screenshots already captured in `/tmp/claude-0/.../scratchpad/shots2/`.
