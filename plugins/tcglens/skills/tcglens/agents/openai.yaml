interface:
  display_name: "TCGlens"
  short_description: "Evidence-backed trading-card decisions"
  brand_color: "#0F766E"
  default_prompt: "Use TCGlens to identify the exact card and compare only supported raw-single evidence."

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "tcglens"
      description: "TCGlens card identity, discovery, comparison, capabilities, and deep-link tools"
      transport: "streamable_http"
      url: "https://tcgpal.vercel.app/mcp"
